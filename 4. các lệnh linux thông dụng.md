# BÀI 4: CÁC LỆNH LINUX THÔNG DỤNG

## 1. pwd (Print Working Directory)

Chức năng chính của nó là hiển thị đường dẫn tuyệt đối (absolute path) của thư mục hiện tại mà bạn đang làm việc trong Terminal.

## 2. whoami (who am I)

Nó trả về ID người dùng (User ID) thực tế của người dùng đang thực hiện lệnh.

## 3. cd (Change Directory)

Đây là lệnh cơ bản và được sử dụng thường xuyên nhất trong Terminal để di chuyển giữa các thư mục (folder) trong hệ thống tập tin.

| Cú pháp                   | Hành động                                              | Ý nghĩa/Ví dụ                                    |
| :------------------------ | :----------------------------------------------------- | :----------------------------------------------- |
| `cd <tên_thư_mục>`        | Di chuyển vào thư mục con.                             | Ví dụ: `cd Documents`                            |
| `cd ..`                   | Di chuyển lên **một cấp** thư mục cha.                 | Ví dụ: Từ `/home/user/doc` trở về `/home/user`   |
| `cd ~` hoặc `cd`          | Trở về **thư mục Home** (`~`) của người dùng hiện tại. | Cách nhanh nhất để về nhà (ví dụ: `/home/user`). |
| `cd -`                    | Quay lại **thư mục làm việc trước đó**.                | Giống như nút "Back" của trình duyệt.            |
| `cd /`                    | Di chuyển đến **thư mục gốc (Root Directory)**.        | Cấp cao nhất của hệ thống tập tin.               |
| `cd /đường/dẫn/tuyệt/đối` | Di chuyển đến một thư mục bằng đường dẫn đầy đủ.       | Ví dụ: `cd /var/log/apache2`                     |

## 4. ls <tên_thư_mục> (List)

Xem nội dung của 1 thư mục nào đó.

- dùng với tên thư mục tương đối: Giả sử bạn đang ở thư mục `/home/user` và muốn xem nội dung của thư mục con Documents: `ls Documents`
- Dùng với Đường dẫn Tuyệt đối: Đây là khi bạn chỉ định đường dẫn đầy đủ, bắt đầu từ thư mục gốc (/), ví dụ: `ls /etc/apache2`
- các tùy chọn:
  - `ls -l` (long list): hiển thị chi tiết
  - `ls -a`: hiển thị tất cả, bao gồm cả các tập tin ẩn
  - `ls -t`(Time): sắp xếp nội dung theo thứ tự từ mới nhất đến cũ nhất -`ls -r`(Reverse): Đảo ngược thứ tự sắp xếp (ví dụ: kết hợp với `-t` thành `ls -tr` để hiển thị mục cũ nhất trước)

## 5. mkdir(Make Directory) : tạo thư mục

- `mkdir -p <đường/dẫn/thư/mục/lồng/nhau>`: Tạo thư mục lồng nhau (Parent Directories): Thường dùng tùy chọn `-p` (parent) để tạo các thư mục cha (parent directories) cùng lúc, nếu chúng chưa tồn tại.
- Nếu bạn muốn tạo thư mục logs bên trong data, mà thư mục data chưa có: `mkdir -p data/logs`

## 6. touch: tạo file

- chức năng chính của touch là tạo 1 hoặc nhiều tập tin trống nếu chúng chưa tồn tại.
- Cập nhật Dấu Thời gian (Timestamp):
  Nếu tập tin đã tồn tại, touch sẽ không thay đổi nội dung của nó mà chỉ cập nhật dấu thời gian của tập tin đó.

        Có ba loại dấu thời gian mà Linux theo dõi cho mỗi tập tin:

        Access Time (-a): Thời điểm tập tin được truy cập lần cuối.

        Modification Time (-m): Thời điểm nội dung tập tin được sửa đổi lần cuối (đây là thời gian mặc định được cập nhật khi dùng touch mà không có tùy chọn).

        Change Time (-c): Thời điểm siêu dữ liệu (metadata) của tập tin bị thay đổi (ví dụ: quyền truy cập).

## 7. rm (Remove): xóa file, folder

- `rm <tên_tệp_tin>`: xóa 1 tệp tin
- `rm -r <tên_thư_mục>`: xóa 1 thư mục

### 🛠️ Các Tùy chọn Quan trọng của Lệnh `rm`

| Tùy chọn | Viết tắt từ (Tiếng Anh)     | Chức năng                                                                                                            |
| :------- | :-------------------------- | :------------------------------------------------------------------------------------------------------------------- |
| **`-r`** | **R**ecursive (Đệ quy)      | Bắt buộc phải dùng khi xóa **thư mục**. Xóa thư mục đó và **tất cả nội dung** bên trong nó.                          |
| **`-f`** | **F**orce (Buộc)            | **Buộc xóa** các tập tin mà **không cần hỏi xác nhận**, ngay cả đối với các tập tin chỉ đọc (read-only).             |
| **`-i`** | **I**nteractive (Tương tác) | Yêu cầu người dùng **xác nhận** (`y` hoặc `n`) trước khi xóa mỗi tập tin hoặc thư mục. Giúp ngăn chặn việc xóa nhầm. |
| **`-v`** | **V**erbose (Dài dòng)      | Hiển thị thông báo chi tiết về những gì đang được thực hiện (in ra tên của từng tập tin bị xóa).                     |

## 8. cp (Copy) Copy file, thư mục

- `cp <nguồn> <đích>` : copy file
- `cp -r <nguồn> <đích>`: copy folder -` cp report.doc old_report.doc`: tạo 1 bản sao tại thư mục hiện tại và đổi tên file đó.

## 9. mv (Move) di chuyển file, thư mục

- Di chuyển tập tin hoặc thư mục từ một vị trí này sang vị trí khác.
- Đổi tên tập tin hoặc thư mục (bằng cách di chuyển nó đến cùng một vị trí nhưng với tên mới).

  `mv <nguồn> <đích>`

## 10. echo: hiển thị (in) một chuỗi ký tự (text) hoặc giá trị của biến ra màn hình Terminal hoặc chuyển hướng (redirect) nó vào một tập tin.

- Chức năng cốt lõi của echo là in lại (echo back) những gì được truyền cho nó:
  `echo <chuỗi_ký_tự_hoặc_biến>`
- Ghi đè (Overwrite) nội dung tập tin: Sử dụng ký hiệu `>` để chuyển hướng đầu ra của echo vào một tập tin. Nếu tập tin đã tồn tại, nội dung cũ sẽ bị xóa và thay thế bằng nội dung mới.
  `echo "Day la dong dau tien." > log.txt`
- Nối thêm (Append) vào tập tin: Sử dụng ký hiệu >> để nối thêm nội dung mới vào cuối tập tin mà không xóa nội dung cũ. `echo "Day la dong thu hai." >> log.txt`

## 11. cat (concatenate) chức năng phổ biến nhất của nó là hiển thị nội dung của một hoặc nhiều tập tin ra màn hình Terminal.

- Hiển thị Nội dung Tập tin `cat <tên_tập_tin>`
- Nối (Concatenate) Tập tin: Bạn có thể dùng cat để nối nội dung của nhiều tập tin lại với nhau và chuyển hướng kết quả vào một tập tin mới. `cat <tập_tin_1> <tập_tin_2> ... > <tập_tin_mới>`. ví dụ: `cat header.txt body.txt footer.txt > full_page.html`. (Nội dung của ba tập tin sẽ được gộp lại và lưu vào full_page.html.)

## 12. `history` xem lịch sử các lệnh đã gõ

## 13. `tail` hiển thị phần cuối của 1 tệp tin

`tail <tên_tập_tin>`

- Nó thường được sử dụng để theo dõi các tập tin nhật ký (log files) đang được ghi thêm vào liên tục, vì các sự kiện mới nhất sẽ luôn xuất hiện ở cuối tập tin.
- Mặc định, lệnh tail sẽ in ra 10 dòng cuối cùng của tập tin được chỉ định ra màn hình Terminal.

Các tùy chọn quan trọng:

- `-n` (number): Hiển thị số dòng được chỉ định tính từ cuối tập tin
- `-f` (follow): theo dõi tập tin
- `-c` (character): hiển thị số lượng ký tự được chỉ định tính từ cuối tập tin
- `tail -n 1 <tên_tập_tin> <tên_tập_tin_mới>`: sao chép 1 dòng cuối sang file mới

## 14. `sudo` cho phép người dùng thông thường được phép thực thi các lệnh với đặc quyền bảo mật của một người dùng khác, thường là người dùng quản trị tối cao (root).

- `sudo -i`: đăng nhập vào user root
