# Bài 15 CI/CD là gì, để làm gì

## CI (Continuous Integration) – Tích hợp liên tục

### ✔️ CI là gì?

CI nghĩa là tự động kiểm tra và build code mỗi khi có ai đó push code lên repository.

### ✔️ CI dùng để làm gì?

- Kiểm tra xem code mới có lỗi hay không

- Build lại toàn bộ dự án tự động

- Chạy unit test, integration test

- Phát hiện lỗi sớm, tránh xung đột giữa các lập trình viên

- Đảm bảo rằng code đưa vào nhánh chính (main / master) luôn ổn định

### ✔️ Ví dụ CI thực tế trong GitLab:

- Khi bạn push code lên GitLab:

- GitLab tự động chạy pipeline

- Kiểm tra syntax

- Chạy test

- Build project (ví dụ mvn clean package)

- Báo “passed” hoặc “failed”

## CD có 2 nghĩa: Continuous Delivery & Continuous Deployment

### 1️⃣ Continuous Delivery – Giao hàng liên tục

Sau khi CI chạy xong → hệ thống tự build ra artifact (file .jar, .war, .zip…)

Deploy lên môi trường staging (test server)

Nhưng để deploy sang production thì cần con người bấm nút đồng ý

👉 Bạn có thể xem nó như:
Build + Test + Chuẩn bị sẵn sàng deploy

### 2️⃣ Continuous Deployment – Triển khai liên tục

Cao cấp hơn Delivery

Sau khi CI chạy xong → deploy luôn lên production

Không cần con người can thiệp

👉 Rất tự động và nhanh, nhưng cần hệ thống ổn định.
